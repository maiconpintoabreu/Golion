{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/zig-out/bin/golion",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        },
    ]
}